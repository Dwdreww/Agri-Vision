// Public frontend configuration.
// Set apiBaseUrl to your deployed Flask backend URL when hosting the frontend on GitHub Pages.
// GitHub Actions overwrites this file during Pages deployment when repository variables/secrets are set.
// apiKey is visible in the browser, so use only a simple app access token here.
window.AGRI_VISION_CONFIG = {
  apiBaseUrl: '',
  apiKey: ''
};
